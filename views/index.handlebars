h1>Burgers!</h1>

<h2>Burgers Eaten!</h2>

<ul>
  {{#each burger}}
    {{#unless eaten}}
      {{> burgers/burger-block eaten=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Cats that are sleepy!</h2>

<ul>
  {{#each burger}}
    {{#if eaten}}
      {{> burgers/burger-block eaten=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>Eat a Burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">Burger Name:</label>
    <input type="text" id="ca" name="name">
  </div>

  <div class="form-group">
    <label for="eaten">Hungry?</label><br>
    <input type="radio" name="hungry" value="1" checked> Ate!<br>
    <input type="radio" name="hungry" value="0"> Eaten
  </div>

  <button type="submit"> Eat Burger</button>
</form>
